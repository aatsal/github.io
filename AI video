<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>AI VISION — Video Gallery</title>
  <link href="https://fonts.googleapis.com/css2?family=Share+Tech+Mono&family=Rajdhani:wght@300;500;700&display=swap" rel="stylesheet" />
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    :root {
      --bg: #05070f;
      --surface: #0c0f1e;
      --card: #0f1429;
      --accent: #00f0ff;
      --accent2: #7b2fff;
      --text: #e0e8ff;
      --muted: #5a6480;
      --border: rgba(0, 240, 255, 0.15);
      --glow: 0 0 20px rgba(0, 240, 255, 0.2);
    }

    body {
      background: var(--bg);
      color: var(--text);
      font-family: 'Rajdhani', sans-serif;
      min-height: 100vh;
      overflow-x: hidden;
    }

    /* ── Grid bg ── */
    body::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image:
        linear-gradient(rgba(0,240,255,0.04) 1px, transparent 1px),
        linear-gradient(90deg, rgba(0,240,255,0.04) 1px, transparent 1px);
      background-size: 48px 48px;
      pointer-events: none;
      z-index: 0;
    }

    /* ── Scan-line overlay ── */
    body::after {
      content: '';
      position: fixed;
      inset: 0;
      background: repeating-linear-gradient(
        0deg,
        transparent,
        transparent 2px,
        rgba(0,0,0,0.08) 2px,
        rgba(0,0,0,0.08) 4px
      );
      pointer-events: none;
      z-index: 0;
    }

    /* ── Nav ── */
    nav {
      position: sticky;
      top: 0;
      z-index: 100;
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 1rem 2.5rem;
      background: rgba(5,7,15,0.85);
      backdrop-filter: blur(12px);
      border-bottom: 1px solid var(--border);
    }

    .logo {
      font-family: 'Share Tech Mono', monospace;
      font-size: 1.1rem;
      color: var(--accent);
      letter-spacing: 0.15em;
      text-shadow: 0 0 12px rgba(0,240,255,0.5);
    }

    .logo span { color: var(--accent2); }

    .nav-tag {
      font-family: 'Share Tech Mono', monospace;
      font-size: 0.7rem;
      color: var(--muted);
      letter-spacing: 0.1em;
    }

    /* ── Hero ── */
    .hero {
      position: relative;
      z-index: 1;
      text-align: center;
      padding: 5rem 1rem 3.5rem;
    }

    .hero-eyebrow {
      font-family: 'Share Tech Mono', monospace;
      font-size: 0.72rem;
      color: var(--accent);
      letter-spacing: 0.3em;
      text-transform: uppercase;
      margin-bottom: 1.2rem;
      opacity: 0.8;
    }

    .hero h1 {
      font-size: clamp(2.5rem, 8vw, 5.5rem);
      font-weight: 700;
      line-height: 1;
      letter-spacing: -0.01em;
      background: linear-gradient(135deg, #ffffff 0%, #a0c4ff 50%, var(--accent) 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      margin-bottom: 1.25rem;
    }

    .hero p {
      font-size: 1.1rem;
      font-weight: 300;
      color: var(--muted);
      max-width: 480px;
      margin: 0 auto;
      letter-spacing: 0.03em;
    }

    .hero-line {
      width: 60px;
      height: 2px;
      background: linear-gradient(90deg, var(--accent2), var(--accent));
      margin: 2rem auto 0;
      border-radius: 2px;
    }

    /* ── Filter bar ── */
    .filter-bar {
      position: relative;
      z-index: 1;
      display: flex;
      gap: 0.6rem;
      justify-content: center;
      flex-wrap: wrap;
      padding: 0 1rem 2.5rem;
    }

    .filter-btn {
      font-family: 'Share Tech Mono', monospace;
      font-size: 0.7rem;
      letter-spacing: 0.12em;
      text-transform: uppercase;
      padding: 0.45rem 1.1rem;
      border: 1px solid var(--border);
      background: transparent;
      color: var(--muted);
      border-radius: 2px;
      cursor: pointer;
      transition: all 0.2s;
    }

    .filter-btn:hover,
    .filter-btn.active {
      border-color: var(--accent);
      color: var(--accent);
      background: rgba(0,240,255,0.05);
      text-shadow: 0 0 8px rgba(0,240,255,0.5);
    }

    /* ── Gallery grid ── */
    .gallery {
      position: relative;
      z-index: 1;
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
      gap: 1.5rem;
      max-width: 1280px;
      margin: 0 auto;
      padding: 0 1.5rem 5rem;
    }

    /* ── Video card ── */
    .card {
      background: var(--card);
      border: 1px solid var(--border);
      border-radius: 4px;
      overflow: hidden;
      cursor: pointer;
      transition: transform 0.25s, border-color 0.25s;
      animation: fadeUp 0.5s ease both;
    }

    .card:hover {
      transform: translateY(-4px);
      border-color: rgba(0,240,255,0.4);
      box-shadow: var(--glow);
    }

    @keyframes fadeUp {
      from { opacity: 0; transform: translateY(20px); }
      to   { opacity: 1; transform: translateY(0); }
    }

    /* Stagger */
    .card:nth-child(1) { animation-delay: 0.05s; }
    .card:nth-child(2) { animation-delay: 0.10s; }
    .card:nth-child(3) { animation-delay: 0.15s; }
    .card:nth-child(4) { animation-delay: 0.20s; }
    .card:nth-child(5) { animation-delay: 0.25s; }
    .card:nth-child(6) { animation-delay: 0.30s; }

    /* Thumbnail area */
    .thumb {
      position: relative;
      aspect-ratio: 16/9;
      background: var(--surface);
      overflow: hidden;
    }

    .thumb img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      display: block;
      transition: transform 0.35s;
    }

    .card:hover .thumb img { transform: scale(1.04); }

    /* Play button */
    .play-btn {
      position: absolute;
      inset: 0;
      display: flex;
      align-items: center;
      justify-content: center;
      background: rgba(5,7,15,0.35);
      opacity: 0;
      transition: opacity 0.2s;
    }

    .card:hover .play-btn { opacity: 1; }

    .play-icon {
      width: 52px;
      height: 52px;
      border: 2px solid var(--accent);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      box-shadow: 0 0 24px rgba(0,240,255,0.5);
    }

    .play-icon::after {
      content: '';
      border-style: solid;
      border-width: 9px 0 9px 16px;
      border-color: transparent transparent transparent var(--accent);
      margin-left: 4px;
    }

    /* Duration badge */
    .badge {
      position: absolute;
      bottom: 10px;
      right: 10px;
      font-family: 'Share Tech Mono', monospace;
      font-size: 0.68rem;
      padding: 3px 8px;
      background: rgba(5,7,15,0.75);
      border: 1px solid var(--border);
      color: var(--accent);
      border-radius: 2px;
      letter-spacing: 0.08em;
    }

    /* Category tag */
    .cat-tag {
      position: absolute;
      top: 10px;
      left: 10px;
      font-family: 'Share Tech Mono', monospace;
      font-size: 0.62rem;
      padding: 3px 8px;
      background: rgba(123,47,255,0.35);
      border: 1px solid rgba(123,47,255,0.5);
      color: #c4a0ff;
      border-radius: 2px;
      letter-spacing: 0.1em;
      text-transform: uppercase;
    }

    /* Card body */
    .card-body {
      padding: 1rem 1.25rem 1.2rem;
    }

    .card-title {
      font-size: 1.05rem;
      font-weight: 700;
      letter-spacing: 0.02em;
      color: var(--text);
      margin-bottom: 0.5rem;
      line-height: 1.3;
    }

    .card-desc {
      font-size: 0.88rem;
      font-weight: 300;
      color: var(--muted);
      line-height: 1.6;
      margin-bottom: 1rem;
    }

    .card-meta {
      display: flex;
      align-items: center;
      gap: 1rem;
    }

    .meta-item {
      font-family: 'Share Tech Mono', monospace;
      font-size: 0.65rem;
      color: var(--muted);
      letter-spacing: 0.08em;
      display: flex;
      align-items: center;
      gap: 5px;
    }

    .meta-dot {
      width: 5px;
      height: 5px;
      border-radius: 50%;
      background: var(--accent);
      opacity: 0.6;
    }

    /* ── Modal ── */
    .modal-overlay {
      display: none;
      position: fixed;
      inset: 0;
      z-index: 200;
      background: rgba(5,7,15,0.92);
      backdrop-filter: blur(8px);
      align-items: center;
      justify-content: center;
      padding: 1.5rem;
    }

    .modal-overlay.open { display: flex; }

    .modal {
      background: var(--card);
      border: 1px solid rgba(0,240,255,0.3);
      border-radius: 4px;
      width: 100%;
      max-width: 760px;
      overflow: hidden;
      animation: modalIn 0.25s ease;
    }

    @keyframes modalIn {
      from { transform: scale(0.95); opacity: 0; }
      to   { transform: scale(1); opacity: 1; }
    }

    .modal-video {
      aspect-ratio: 16/9;
      width: 100%;
      background: #000;
    }

    .modal-video iframe {
      width: 100%;
      height: 100%;
      border: none;
      display: block;
    }

    .modal-info {
      padding: 1.25rem 1.5rem 1.5rem;
      display: flex;
      align-items: flex-start;
      justify-content: space-between;
      gap: 1rem;
    }

    .modal-title {
      font-size: 1.1rem;
      font-weight: 700;
      margin-bottom: 0.3rem;
    }

    .modal-sub {
      font-size: 0.85rem;
      font-weight: 300;
      color: var(--muted);
    }

    .modal-close {
      font-family: 'Share Tech Mono', monospace;
      font-size: 0.7rem;
      padding: 0.45rem 1rem;
      border: 1px solid var(--border);
      background: transparent;
      color: var(--muted);
      cursor: pointer;
      border-radius: 2px;
      letter-spacing: 0.1em;
      white-space: nowrap;
      transition: all 0.2s;
      flex-shrink: 0;
    }

    .modal-close:hover {
      border-color: var(--accent);
      color: var(--accent);
    }

    /* ── Footer ── */
    footer {
      position: relative;
      z-index: 1;
      text-align: center;
      padding: 2rem;
      border-top: 1px solid var(--border);
      font-family: 'Share Tech Mono', monospace;
      font-size: 0.65rem;
      color: var(--muted);
      letter-spacing: 0.15em;
    }
  </style>
</head>
<body>

  <!-- Nav -->
  <nav>
    <div class="logo">AI<span>::</span>VISION</div>
    <div class="nav-tag">// VIDEO GALLERY v1.0</div>
  </nav>

  <!-- Hero -->
  <section class="hero">
    <p class="hero-eyebrow">// Artificial Intelligence · Generated Media</p>
    <h1>AI VIDEO<br>GALLERY</h1>
    <p>A showcase of AI-generated visual worlds — explore the frontier of machine creativity.</p>
    <div class="hero-line"></div>
  </section>

  <!-- Filter bar -->
  <div class="filter-bar">
    <button class="filter-btn active" onclick="filterCards('all', this)">ALL</button>
    <button class="filter-btn" onclick="filterCards('sora', this)">SORA</button>
    <button class="filter-btn" onclick="filterCards('runway', this)">RUNWAY</button>
    <button class="filter-btn" onclick="filterCards('kling', this)">KLING</button>
    <button class="filter-btn" onclick="filterCards('luma', this)">LUMA</button>
  </div>

  <!-- Gallery -->
  <main class="gallery" id="gallery">

    <!-- CARD 1 -->
    <article class="card" data-cat="sora" onclick="openModal('https://www.youtube.com/embed/HK6y8DAPN_0', 'Neon City at Dawn', 'Sora · 2024')">
      <div class="thumb">
        <img src="https://images.unsplash.com/photo-1518770660439-4636190af475?w=600&q=80" alt="Neon City at Dawn" loading="lazy" />
        <div class="play-btn"><div class="play-icon"></div></div>
        <span class="badge">1:02</span>
        <span class="cat-tag">SORA</span>
      </div>
      <div class="card-body">
        <h2 class="card-title">Neon City at Dawn</h2>
        <p class="card-desc">A hyper-real cityscape emerging from darkness — lights bloom across wet streets as the sun breaks the horizon.</p>
        <div class="card-meta">
          <span class="meta-item"><span class="meta-dot"></span>SORA</span>
          <span class="meta-item">2024</span>
          <span class="meta-item">720p</span>
        </div>
      </div>
    </article>

    <!-- CARD 2 -->
    <article class="card" data-cat="runway" onclick="openModal('https://www.youtube.com/embed/HK6y8DAPN_0', 'Ocean of Data', 'Runway Gen-3 · 2024')">
      <div class="thumb">
        <img src="https://images.unsplash.com/photo-1451187580459-43490279c0fa?w=600&q=80" alt="Ocean of Data" loading="lazy" />
        <div class="play-btn"><div class="play-icon"></div></div>
        <span class="badge">0:48</span>
        <span class="cat-tag">RUNWAY</span>
      </div>
      <div class="card-body">
        <h2 class="card-title">Ocean of Data</h2>
        <p class="card-desc">Streams of information visualised as a bioluminescent ocean — data packets pulse like deep-sea creatures.</p>
        <div class="card-meta">
          <span class="meta-item"><span class="meta-dot"></span>RUNWAY</span>
          <span class="meta-item">2024</span>
          <span class="meta-item">1080p</span>
        </div>
      </div>
    </article>

    <!-- CARD 3 -->
    <article class="card" data-cat="kling" onclick="openModal('https://www.youtube.com/embed/HK6y8DAPN_0', 'Synthetic Forest', 'Kling AI · 2024')">
      <div class="thumb">
        <img src="https://images.unsplash.com/photo-1542281286-9e0a16bb7366?w=600&q=80" alt="Synthetic Forest" loading="lazy" />
        <div class="play-btn"><div class="play-icon"></div></div>
        <span class="badge">1:15</span>
        <span class="cat-tag">KLING</span>
      </div>
      <div class="card-body">
        <h2 class="card-title">Synthetic Forest</h2>
        <p class="card-desc">A forest grown from algorithms — crystalline trees shift through seasons in seconds, defying natural law.</p>
        <div class="card-meta">
          <span class="meta-item"><span class="meta-dot"></span>KLING</span>
          <span class="meta-item">2024</span>
          <span class="meta-item">4K</span>
        </div>
      </div>
    </article>

    <!-- CARD 4 -->
    <article class="card" data-cat="luma" onclick="openModal('https://www.youtube.com/embed/HK6y8DAPN_0', 'Zero Gravity', 'Luma Dream Machine · 2024')">
      <div class="thumb">
        <img src="https://images.unsplash.com/photo-1534796636912-3b95b3ab5986?w=600&q=80" alt="Zero Gravity" loading="lazy" />
        <div class="play-btn"><div class="play-icon"></div></div>
        <span class="badge">0:58</span>
        <span class="cat-tag">LUMA</span>
      </div>
      <div class="card-body">
        <h2 class="card-title">Zero Gravity</h2>
        <p class="card-desc">Everyday objects drift in weightless orbits — physics dismantled and rebuilt into a poetic new order.</p>
        <div class="card-meta">
          <span class="meta-item"><span class="meta-dot"></span>LUMA</span>
          <span class="meta-item">2024</span>
          <span class="meta-item">1080p</span>
        </div>
      </div>
    </article>

    <!-- CARD 5 -->
    <article class="card" data-cat="sora" onclick="openModal('https://www.youtube.com/embed/HK6y8DAPN_0', 'Quantum Bloom', 'Sora · 2025')">
      <div class="thumb">
        <img src="https://images.unsplash.com/photo-1462331940025-496dfbfc7564?w=600&q=80" alt="Quantum Bloom" loading="lazy" />
        <div class="play-btn"><div class="play-icon"></div></div>
        <span class="badge">1:30</span>
        <span class="cat-tag">SORA</span>
      </div>
      <div class="card-body">
        <h2 class="card-title">Quantum Bloom</h2>
        <p class="card-desc">A flower blossoms through impossible colour spaces — petals unfolding across dimensions beyond three.</p>
        <div class="card-meta">
          <span class="meta-item"><span class="meta-dot"></span>SORA</span>
          <span class="meta-item">2025</span>
          <span class="meta-item">4K</span>
        </div>
      </div>
    </article>

    <!-- CARD 6 -->
    <article class="card" data-cat="runway" onclick="openModal('https://www.youtube.com/embed/HK6y8DAPN_0', 'Machine Dreams', 'Runway Gen-3 · 2025')">
      <div class="thumb">
        <img src="https://images.unsplash.com/photo-1470790376778-a9fbc86d70e2?w=600&q=80" alt="Machine Dreams" loading="lazy" />
        <div class="play-btn"><div class="play-icon"></div></div>
        <span class="badge">2:04</span>
        <span class="cat-tag">RUNWAY</span>
      </div>
      <div class="card-body">
        <h2 class="card-title">Machine Dreams</h2>
        <p class="card-desc">What does an AI dream when left to its own devices? Abstract narratives form and dissolve like thought itself.</p>
        <div class="card-meta">
          <span class="meta-item"><span class="meta-dot"></span>RUNWAY</span>
          <span class="meta-item">2025</span>
          <span class="meta-item">1080p</span>
        </div>
      </div>
    </article>

  </main>

  <!-- Modal -->
  <div class="modal-overlay" id="modal" onclick="closeModalOutside(event)">
    <div class="modal">
      <div class="modal-video">
        <iframe id="modal-iframe" src="" allowfullscreen></iframe>
      </div>
      <div class="modal-info">
        <div>
          <p class="modal-title" id="modal-title"></p>
          <p class="modal-sub" id="modal-sub"></p>
        </div>
        <button class="modal-close" onclick="closeModal()">✕ CLOSE</button>
      </div>
    </div>
  </div>

  <!-- Footer -->
  <footer>
    &copy; 2025 AI::VISION · ALL RIGHTS RESERVED · REPLACE VIDEOS WITH YOUR OWN YOUTUBE LINKS
  </footer>

  <script>
    function openModal(src, title, sub) {
      document.getElementById('modal-iframe').src = src;
      document.getElementById('modal-title').textContent = title;
      document.getElementById('modal-sub').textContent = sub;
      document.getElementById('modal').classList.add('open');
      document.body.style.overflow = 'hidden';
    }

    function closeModal() {
      document.getElementById('modal').classList.remove('open');
      document.getElementById('modal-iframe').src = '';
      document.body.style.overflow = '';
    }

    function closeModalOutside(e) {
      if (e.target === document.getElementById('modal')) closeModal();
    }

    document.addEventListener('keydown', e => { if (e.key === 'Escape') closeModal(); });

    function filterCards(cat, btn) {
      document.querySelectorAll('.filter-btn').forEach(b => b.classList.remove('active'));
      btn.classList.add('active');
      document.querySelectorAll('.card').forEach(card => {
        const show = cat === 'all' || card.dataset.cat === cat;
        card.style.display = show ? '' : 'none';
      });
    }
  </script>
</body>
</html>
